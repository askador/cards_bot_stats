/**
 * JQuery для текущего онлайна 
 */
$(document).ready(function(){
    online = $('.details__text__entity');
  
    /**
    var socket = io()
  
    socket.emit('home_get_online');
    socket.once('home_online', function(data) {
        var games = data['games']
        var users = data['users']

        var html = `Игр: ${games}<br>
                    Пользователи: ${users}<br>`
                    
        online.html(html);
    })
*/
  
})