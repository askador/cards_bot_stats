/**
 * JQuery для текущего онлайна 
 */
$(document).ready(function(){
    general__online = $('.general__online');
  
    /**
    var socket = io()
  
    socket.emit('games_general_get_online');
    socket.once('games_general_online', function(data) {
        var games = data['games']
        var users = data['users']

        var html = `Игр: ${games}<br>
                    Пользователи: ${users}<br>`
                    
        general__online.html(html);
    })
*/
  
})