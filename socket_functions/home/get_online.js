/**
 * Функция для передачи текущего онлайна
 * @param {*} socket 
 */
module.exports = function get_online(socket) {

    /**
     * 
     * Достать количество пользователей, у которых последняя активность находится в 10 секундах от текущего времени
     * и количество активных игры сейчас
     * 
     */

  //   db.query("SELECT count(id) FROM test_stats", function (err, results) { 

  //     var data = {
  //       'games': results[0]['count(id)'],

  //       'users': results[0]['count(id)'],
  //     }

    
    
  //     socket.emit('home_online', data)

  // })
  
}