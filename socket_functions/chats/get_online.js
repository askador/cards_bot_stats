/**
 * Функция для передачи текущего онлайна чатов
 * @param {*} socket 
 */
module.exports = function get_online(socket) {

    /**
     * 
     * Достать количество чатов, где последняя активность находится в 10 секундах от текущего времени
     * 
     */


  //   db.query("SELECT count(id) FROM test_stats", function (err, results) { 


  //     var data = {
  //       'games': results[0]['count(id)'],
  
  //       'users': results[0]['count(id)'],
  //     }

      
  //     socket.emit('chats_online', data)
  // })

}
